{
  "name": "true-future-backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "cd frontend && npm run build",
    "build:frontend": "cd frontend && npm run build"
  },
  "devDependencies": {
    "wrangler": "^4.59.2"
  },
  "type": "module",
  "dependencies": {
    "astronomy-engine": "^2.1.19"
  }
}
